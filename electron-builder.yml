appId: com.repsil.app
productName: Repsil
directories:
  output: release
win:
  icon: build/icon.ico
# Auto-update feed. `npm run release` (with GH_TOKEN set) uploads the installer
# + latest.yml here; the packaged app reads it to update itself. See BUILD.md.
publish:
  provider: github
  owner: voidnor1807-maker
  repo: repsil
files:
  - out/**
  - resources/**
# Native binaries and worker/wasm assets cannot be loaded from inside the asar
# archive — they must sit as loose files. (K2)
asarUnpack:
  - "**/*.node"
  - "**/node_modules/better-sqlite3/**"
  - "**/node_modules/@napi-rs/**"
  - "**/node_modules/tesseract.js/**"
  - "**/node_modules/tesseract.js-core/**"
  - "**/node_modules/pdfjs-dist/**"
# Ship OCR language data unpacked so OCR works fully offline. Place
# eng.traineddata + ara.traineddata in resources/tessdata/ (see the README
# there). (K1)
extraResources:
  - from: resources/tessdata
    to: tessdata
    filter:
      - "**/*.traineddata"
